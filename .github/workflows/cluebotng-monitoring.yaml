name: 'Execute deployment for cluebotng-monitoring'
on: { push: { branches: [ main ], paths: [ 'cluebotng-monitoring.yaml', 'config/network-policies/cluebotng-monitoring.yaml', 'config/web-services/cluebotng-monitoring.yaml', '.github/workflows/cluebotng-monitoring.yaml' ] }, workflow_dispatch: { } }
concurrency:
  group: cluebotng-monitoring
jobs:
  update-network-policies:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: cluebotng/ci-execute-fabric@main
        with:
          user: 'cluebotng-monitoring'
          task: update-network-policies
          ssh_key: ${{ secrets.CI_SSH_KEY }}
  update-component-config:
    runs-on: ubuntu-latest
    needs: [update-network-policies]
    steps:
      - uses: actions/checkout@v4
      - uses: cluebotng/ci-execute-fabric@main
        with:
          user: 'cluebotng-monitoring'
          task: update-component-config
          ssh_key: ${{ secrets.CI_SSH_KEY }}
  execute-deployment:
    runs-on: ubuntu-latest
    needs: [update-network-policies, update-component-config]
    steps:
      - uses: actions/checkout@v4
      - uses: cluebotng/ci-execute-fabric@main
        with:
          user: 'cluebotng-monitoring'
          task: execute-deployment
          ssh_key: ${{ secrets.CI_SSH_KEY }}
  update-webservice:
    runs-on: ubuntu-latest
    needs: [update-network-policies, update-component-config, execute-deployment]
    steps:
      - uses: actions/checkout@v4
      - uses: cluebotng/ci-execute-fabric@main
        with:
          user: 'cluebotng-monitoring'
          task: update-webservice
          ssh_key: ${{ secrets.CI_SSH_KEY }}
